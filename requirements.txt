# ── Core / numerics ──
numpy
opencv-python-headless        # headless: no GUI libs needed on servers
Pillow
scikit-image
scipy
PyWavelets
scikit-learn

# ── Deep learning ──
# For deployment install the CPU build to keep the image small:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
torchvision
transformers

# ── Document / forensics IO ──
exifread
PyMuPDF
pytesseract                   # OCR fallback (requires the tesseract-ocr binary)

# ── API ──
fastapi
uvicorn[standard]
gunicorn
python-multipart
pydantic

# ── Tests ──
pytest
pytest-cov
httpx                         # required by FastAPI TestClient
